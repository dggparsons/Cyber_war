# Default Admin Credentials

During seeding/startup the backend creates a Game Master account:

- Email: `admin@warroom.local`
- Temporary password: `ChangeMe123!`

Change this password via `/api/auth/login` + admin panel before running the live game.
